<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tabacohookah">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".tareas.AddTabaco"></activity>
        <activity
            android:name=".utilidades.HomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".tareas.RegistrarseActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".tareas.IniciarSesionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".utilidades.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>